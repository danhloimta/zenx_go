'use client';
import { useParams } from 'next/navigation'; import { useEffect, useState } from 'react'; import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import type { FeatureConfig, GamePageConfig, ThemeConfig } from '@zenx-go/api-client'; import { api } from '@/lib/api'; import { getErrorMessage } from '@/lib/errors'; import { Button } from '@/components/ui/button'; import { GamePresentationEditor, type PresentationValue } from '@/components/admin-content/game-presentation-editor';
export default function GamePresentationPage() { const { subdomain } = useParams<{ subdomain: string }>(); const client = useQueryClient(); const [value, setValue] = useState<PresentationValue | null>(null); const [savedAt, setSavedAt] = useState(''); const context = useQuery({ queryKey: ['game-admin', 'context', subdomain], queryFn: () => api.gameAdmin.context(subdomain), retry: false }); const presentation = useQuery({ queryKey: ['game-admin', 'presentation', context.data?.game.id], queryFn: () => api.gameAdmin.content.presentation(context.data!.game.id), enabled: Boolean(context.data), retry: false }); useEffect(() => { if (presentation.data) { setValue({ themeConfig: JSON.parse(presentation.data.themeConfig) as ThemeConfig, featureConfig: JSON.parse(presentation.data.featureConfig) as FeatureConfig, pageConfig: JSON.parse(presentation.data.pageConfig) as GamePageConfig }); setSavedAt(presentation.data.updatedAt); } }, [presentation.data]); const save = useMutation({ mutationFn: () => api.gameAdmin.content.updatePresentation(context.data!.game.id, { expectedUpdatedAt: savedAt, ...value! }), onSuccess: (game) => { setSavedAt(game.updatedAt); void client.invalidateQueries({ queryKey: ['game-admin', 'presentation', context.data?.game.id] }); } }); if (presentation.isLoading || !value || !context.data) return <p className="text-sm text-slate-500">Đang tải giao diện…</p>; if (presentation.isError || !presentation.data) return <p className="text-sm text-red-600">{getErrorMessage(presentation.error, 'Không thể tải giao diện game.')}</p>; return <div className="space-y-5"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-black">Giao diện game</h1><p className="mt-1 text-sm text-slate-500">Cấu hình trực quan chỉ áp dụng cho {context.data.game.name}.</p></div><Button disabled={save.isPending} onClick={() => save.mutate()}>{save.isPending ? 'Đang lưu…' : 'Lưu giao diện'}</Button></div>{save.isError ? <p className="text-sm text-red-600">{getErrorMessage(save.error, 'Không thể lưu. Dữ liệu có thể đã được người khác cập nhật.')}</p> : null}<GamePresentationEditor value={value} preset={presentation.data.themePreset} onChange={setValue} /></div>; }
